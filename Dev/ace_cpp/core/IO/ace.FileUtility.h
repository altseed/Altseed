#pragma once

#include <vector>

namespace ace
{
	class FileUtility
	{
	public:
		//template<typename T>
		//static void SubstitutionRange(std::vector<T> buffer, const size_t bufferOffset, const T* elements, const size_t elementsSize)
		//{
		//	for (size_t i(0); i < elementsSize; ++i)
		//	{
		//		buffer[bufferOffset + i] = elements[i];
		//	}
		//}
		//template<typename T1, typename T2>
		//static void SubstitutionRange(std::vector<T1> buffer, const size_t bufferOffset, const T2* elements, const size_t elementsSize)
		//{
		//	SubstitutionRange(buffer, bufferOffset, reinterpret_cast<T1*>(elements), elementsSize*sizeof(T2));
		//}
	};
}